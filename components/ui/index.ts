/**
 * UI COMPONENTS BARREL EXPORT
 * ====================================
 * Punto central de exportación para todos los componentes UI.
 * Permite importaciones limpias: import { Button, Input } from '@/components/ui'
 */

export { Button } from './Button';
export { Input } from './Input';
export { Card } from './Card';
